#
# Show that each command is executed from separate shell. For the "if" clause,
# both "echo" commands will emit the same PID - .i.e. PID for the single shell.
#

all:
	echo $$$$
	@echo $$$$
	@if true; then \
		echo $$$$; \
		echo $$$$; \
	fi
