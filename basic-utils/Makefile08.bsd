# 
# Some make programs have directives for conditional processing of input file.
# Run like this:
#
#	make -f Makefile09
#	D=xxx -f Makefile09
#
# NOTE: This works only with BSD make and the like.
#

all:
.       if defined(D)
	@echo ${D}
.       else
	@echo "D not defined"
.       endif
